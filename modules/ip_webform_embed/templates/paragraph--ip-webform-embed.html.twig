{#
/**
 * @file
 * IP Webform Embed paragraph bundle.
 *
 * @ingroup themeable
 */
#}

<section>
  {% if content.field_ip_we_title[0] %}
    <h2>{{ content.field_ip_we_title }}</h2>
  {% endif %}

  {{ content.field_ip_we_webform }}

  {{ content|without('field_ip_we_title', 'field_ip_we_webform') }}
</section>
